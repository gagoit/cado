= bootstrap_form_for @team_photo, :html => { :multipart => true } do |f|
  - if @team_photo.errors.any?
    #error_explanation
      %h2= "#{pluralize(@team_photo.errors.count, "error")} prohibited this team_photo from being saved:"
      %ul
        - @team_photo.errors.full_messages.each do |msg|
          %li= msg

  = f.select :team_id, (Team.all.map { |e| [e.name, e.id] }), :include_blank => false

  .fileinput.fileinput-new{"data-provides" => "fileinput"}
    .fileinput-preview.thumbnail{"data-trigger" => "fileinput", :style => "width: 200px; height: 150px;"}
    %div
      %span.btn.btn-default.btn-file
        %span.fileinput-new Select image
        %span.fileinput-exists Change
        %input{id: "team_photo_image", name: "team_photo[image]", :type => "file"}/
      %a.btn.btn-default.fileinput-exists{"data-dismiss" => "fileinput", :href => "#"} Remove

  .actions
    = f.submit 'Save'
